services:
  satellite-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    image: satellite-tracker:latest
    container_name: satellite-tracker
    ports:
      - "8080:80"
    restart: unless-stopped
